name: Release Charm to Edge and Publish Libraries

on:
  workflow_dispatch: {}
  push:
    branches:
      - main

jobs:
  release:
    uses: canonical/observability/.github/workflows/charm-release.yaml@fix/build-for-arm
    secrets: inherit
    with:
      provider: lxd
      build-for-arm: true
